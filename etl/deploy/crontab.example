# Cron alternative to the systemd timer (crontab -e on the server).
# Hourly incremental run; stdout/stderr appended to a logfile.
17 * * * * cd /opt/arena-etl && ./.venv/bin/python arena_etl.py run >> /var/log/arena-etl.log 2>&1
